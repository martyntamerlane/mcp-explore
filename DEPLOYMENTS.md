# Deployments

Every release to GitHub Pages gets a row (deploys happen automatically on push to main via `.github/workflows/deploy.yml`). Keep entries terse.

| Date | What shipped | Status | Note |
|------|--------------|--------|------|
| 2026-08-25 | Initial release: scaffold, demo MCP server, connection layer, proof-harness UI (PR #1) | ok | First Pages deploy; run 32801612974, ~30s |
| 2026-08-25 | UI v1: flow view, detail panel, dune mode (PR #2) | ok | Run 32856035432, ~1m2s; row backfilled 2026-08-29 |
| 2026-08-29 | Luminous deck + tool-first workspace: one chrome band, browse column, permanent workspace, one-click run, schema-driven input forms, dark mode (PR #3) | ok | Run 33262179259, ~49s; verified live bundle serves the new UI |
| 2026-08-29 | Visual system tightening (type/space scale, single-column results, landing priority) + markdown detection and rendering (PR #4) | ok | Run 33267649751, ~51s; verified live: scale tokens present, prism on the Connect door, a text/markdown resource renders with its raw toggle, no console errors |
| 2026-08-29 | Reading pass: one 780px measure across the workspace, markdown type cut to four voices, JetBrains Mono pinned, `<details>` HTML dropped, instructions clamp, Inter + prism "b" + favicon (PR #5) | ok | Run 33270510968, ~54s; verified live: `--measure-read: 780px` with paragraph and `pre` both at 780 in a 1140 workspace, `--mono` resolving to JetBrains Mono Variable, favicon present, deepwiki's `<summary>` no longer leaking into the result, no console errors |
| 2026-08-30 | Interaction roadmap: addressable selection and keyboard navigation (S1), run history and honest progress (S3), the result outline (S4), command mode and shortcut legibility (S2), three zones in the workspace, the landing's example servers and rotating hero line (PR #6) | ok | Run 33323460047, ~53s. **Row backfilled 2026-08-30** — it was missed at the time; the deploy itself succeeded, and the live site was exercised the same day while verifying PR #7 (example chips render, a real server connects, no console errors) |
| 2026-08-30 | Connection diagnostics: one verdict derived from the failure instead of a fixed CORS lecture, a `no-cors` reachability probe, 401 seeding the headers box, corrected CORS advice + `curl` handshake, Microsoft Learn added; TODO-9 closed (bounded pagination, attempt phases) (PR #7) | ok | Run 33330939533, ~60s; verified live: the CORS refusal verdict against `knowledge-mcp.global.api.aws` (probe-confirmed), and the two verdicts that need a real HTTPS origin and could not be tested locally — private-host (`http://192.168.1.50`) and mixed-content (`http://mcp.deepwiki.com`) — plus five example chips and a clean Microsoft Learn connect, no console errors |
| 2026-08-30 | Security review hardening: shell-quoted handshake snippet, `?server=` consent gate, markdown scan budget, CSP meta tag, bounded tool-call timeout (ISSUE-11 → ISSUE-15) (PR #8) | ok | Run 33337549626, ~51s. **Row backfilled 2026-09-01** — missed at the time |
| 2026-08-30 | Outline and wide blocks: the result outline widens with the viewport and the reading column centres with it (ISSUE-16, ISSUE-19); **and, unintentionally, selection moving from `?server=` to `#server=` (TODO-31 Session A)**, which was authored on a parallel branch in the same checkout and rode along in this PR (PR #9) | ok | Run 33338500491, ~1m7s. **Row backfilled 2026-09-01** — missed at the time, and worth the note: a user-facing URL-contract change shipped under a PR title about layout. Verified live 2026-09-01: a `#server=` link fills the address box, selecting a subject writes `#tool=…`, and the document request carries no query string |
